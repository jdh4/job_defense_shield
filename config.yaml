---
excess-cpu-memory:
  tb_hours_per_day: 10
  ratio_threshold: 0.2
  mean_ratio_threshold: 0.2
  median_ratio_threshold: 0.2
  clusters:
    - della
  partition:
    - cpu
  cores_per_node: 28
  excluded_users:
    - aturing
    - einstein
...
